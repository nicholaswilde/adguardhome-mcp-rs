# Upstream DNS & Cache

## Objective
Allow configuration of DNS settings and cache control.

## Scope
- Get/Set upstream DNS servers.
- Clear DNS cache.

## Tools to be added
- `get_dns_config`: Retrieve current DNS settings.
- `set_dns_config`: Update upstream servers and other settings.
- `clear_dns_cache`: Flush the cache.
