# AdGuard Home MCP Server Configuration Example

# --- Single Instance Configuration (Legacy/Default) ---
# These settings will be used to create a default instance named "default"
# if no [[instances]] are defined.

# Host of your AdGuard Home instance (Required)
adguard_host = "192.168.1.10"

# Port of your AdGuard Home instance (Optional, Default: 3000)
adguard_port = 8080

# Disable SSL certificate verification (Useful for self-signed certs)
# Default: true
no_verify_ssl = true

# AdGuard Home Authentication (Optional if not enabled on instance)
adguard_username = "admin"
adguard_password = "yourpassword"

# --- Multi-Instance Configuration ---
# You can define multiple instances. The first one will be the default.
# All tools accept an optional "instance" argument (name or index).

# [[instances]]
# name = "primary"
# url = "http://192.168.1.10:8080"
# username = "admin"
# password = "yourpassword"
# no_verify_ssl = true

# [[instances]]
# name = "secondary"
# url = "http://192.168.1.20:3000"
# api_key = "your-api-key"

# --- Synchronization Settings ---
# Automatically sync configuration from the master (first instance) to replicas.

# Interval for automated background sync in seconds. Default: 3600
# sync_interval_seconds = 3600

# Default sync mode: "additive-merge" (safe) or "full-overwrite" (destructive).
# Default: "additive-merge"
# default_sync_mode = "additive-merge"

# [[replicas]]
# url = "http://192.168.1.11:3000"
# api_key = "replica-api-key-1"

# --- MCP Server Settings ---
# Transport mode: "stdio" (local) or "http" (network)
# Default: "stdio"
mcp_transport = "stdio"

# HTTP Transport Settings (Only used if mcp_transport = "http")
# Port to listen on. Default: 3000
http_port = 3000

# Bearer token for HTTP security (Recommended for HTTP mode)
# Clients must provide this in the Authorization header: "Bearer <token>"
http_auth_token = "your-secure-token"

# --- Performance & Token Optimization ---
# Enable "Lazy Mode" to initially expose fewer tools to save AI context tokens.
# Default: false
lazy_mode = false

# --- Logging ---
# Log level: "error", "warn", "info", "debug", "trace"
# Default: "info"
log_level = "info"
